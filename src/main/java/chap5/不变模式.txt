不变模式的主要使用场景需要满足一下2个条件:
    1. 当对象创建后，其内部状态和数据不再发生任何变化
    2. 对象需要被共享，被多线程频繁访问



在java语言中，不变模式的实现很简单，为确保对象被创建后，不发生任何改变，并保证不变模式正常工作，只需注意以下4点:
    1.去除setter方法以及所有修改自身属性的方法
    2.将所有属性设置为私有，并用final标记，确保其不可修改
    3.确保没有子类可以重载修改它的行为
    4.有一个可以创建完整对象的构造函数



    示例:不可变产品对象